// Theme
export type Theme = "light" | "dark";

export interface ThemeColor {
  mainBg: string;
  mainText: string;
  subBg: string;
  subText: string;
  primary: string;
  secondary: string;
  red: string;
  orange: string;
  blue: string;
  green: string;
}
